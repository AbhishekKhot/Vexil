{
  "createEnvironment": {
    "tags": ["Environments"],
    "summary": "Create environment",
    "security": [{ "bearerAuth": [] }],
    "body": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string" }
      }
    }
  },

  "listEnvironments": {
    "tags": ["Environments"],
    "summary": "List environments",
    "security": [{ "bearerAuth": [] }]
  },

  "rotateApiKey": {
    "tags": ["Environments"],
    "summary": "Rotate API key",
    "security": [{ "bearerAuth": [] }]
  },

  "deleteEnvironment": {
    "tags": ["Environments"],
    "summary": "Delete environment",
    "security": [{ "bearerAuth": [] }]
  }
}
