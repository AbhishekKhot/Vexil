{
  "getFlagConfig": {
    "tags": ["Flag Config"],
    "summary": "Get flag config for an environment",
    "security": [{ "bearerAuth": [] }]
  },

  "setFlagConfig": {
    "tags": ["Flag Config"],
    "summary": "Create or update flag config for an environment",
    "security": [{ "bearerAuth": [] }],
    "body": {
      "type": "object",
      "required": ["isEnabled"],
      "properties": {
        "isEnabled":    { "type": "boolean" },
        "strategyType": {
          "type": "string",
          "enum": ["boolean", "rollout", "targeted_rollout", "user_targeting", "attribute_matching", "ab_test", "time_window", "prerequisite"]
        },
        "strategyConfig": {
          "type": "object",
          "description": "Strategy-specific payload — shape depends on strategyType."
        },
        "scheduledAt": {
          "type": "string",
          "nullable": true,
          "description": "ISO-8601 datetime at which scheduledConfig should be applied."
        },
        "scheduledConfig": {
          "type": "object",
          "nullable": true,
          "description": "Config snapshot to apply when scheduledAt arrives."
        }
      }
    }
  }
}
