{
  "createProject": {
    "tags": ["Projects"],
    "summary": "Create project",
    "security": [{ "bearerAuth": [] }],
    "body": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string" },
        "description": { "type": "string" }
      }
    }
  },

  "listProjects": {
    "tags": ["Projects"],
    "summary": "List projects",
    "security": [{ "bearerAuth": [] }]
  },

  "getProject": {
    "tags": ["Projects"],
    "summary": "Get project",
    "security": [{ "bearerAuth": [] }]
  },

  "deleteProject": {
    "tags": ["Projects"],
    "summary": "Delete project",
    "security": [{ "bearerAuth": [] }]
  }
}
